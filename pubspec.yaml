name: test_dart_package
description: Test Dart package.
publish_to: none

directories:
  lib: path/to/lib

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
